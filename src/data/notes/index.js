export const allNotes = [];
export const noteCategories = {};
export const courses = [];

// Helper Functions
export const getCourseById = (id) => null;
export const getTopicBySlug = (courseId, slug) => null;

export default allNotes;
